// NOTE: Unused component — not imported anywhere in the app.
// Previously imported tMUST1.png, promotion1.webp, promotion2.webp,
// summer_promotion.webp, none of which exist in public/ anymore.
// Left as a stub so it doesn't break TypeScript/build if the folder
// is ever re-scanned. Re-add real promo images + markup here when
// the promotions section is ready to ship.
export default async function Promotions() {
  return null;
}
